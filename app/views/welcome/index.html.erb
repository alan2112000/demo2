<p><%= link_to 'Hello!', welcome_say_hello_path %></p>

<div class="row"> 
	<% @p.each do |product| %> 
		<div class="col-sm-6 col-md-4">
			<div class="thumbnail">
				<img data-src="holder.js/300x200" alt="... ">
				<div class="caption">
					<h3><%= product.name %> </h3>
					<p><%= product.description %> </p> 
					<p><a href="#" class="btn btn-primary" role="button">$xxxxx</a> <a href="#" class="btn btn-default" role="button">Read More</a></p>
				</div>
			</div>
			<p><%= link_to p.name , product_path(p) %></p> 
			<p><%= link_to 'edit' , edit_product_path(product) %></p> 
			<p><%= button_to 'delete' , product_path(product),:method => :delete %> <p> 
		</div>   

	<%end%> 
	<%= link_to "New event", new_product_path %> 
